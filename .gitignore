# Editor / IDE
*.swp
*.swo
*~
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.*

# Cyrius build
build/

# lib/ is populated by `cyrius deps` from cyrius.cyml [deps].stdlib — a
# build artifact, not source. Never commit; never replace with a symlink
# to a cyrius checkout (see CLAUDE.md "Dependency wiring" for why).
/lib/

deps/wgpu-native/
deps/libwgpu_shim.so
deps/*.o
deps/*.zip
deps/print_offsets
deps/test_c
deps/test_c.c
deps/test_shim
deps/test_shim.c

# programs/diagnostics/radv_capture_triangle `make snoop` outputs: raw amdgpu IB
# captures and the LD_PRELOAD snoop shim built from ibsnoop.c.
programs/diagnostics/radv_capture_triangle/snoop/
programs/diagnostics/radv_capture_triangle/ibsnoop.so

# Coverage
tarpaulin-report.json
lcov.info
*.profraw
coverage/
/-0
